version: '3'

services:
  nginx:
    image: nginx:latest
    ports:
      - 81:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - nginx-network

  fastAPIApp1:
    image: fastAPIApp1
    networks:
      - nginx-network

  fastAPIApp2:
    image: fastAPIApp2
    networks:
      - nginx-network

networks:
  nginx-network:
    external:
      name: nginx-network
